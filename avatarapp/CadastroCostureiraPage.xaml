<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="avatarapp.CadastroCostureiraPage"
             Title="Cadastro Costureira">

    <ScrollView>
        <StackLayout Padding="20" Spacing="10">
            <Label Text="Cadastro Costureira" 
                   FontSize="Large" 
                   HorizontalOptions="Center"
                   FontAttributes="Bold" />

            <Label Text="Nome:" />
            <Picker x:Name="NomePicker" />

            <Label Text="Telefone:" />
            <Entry x:Name="TelefoneEntry" Keyboard="Telephone" />

            <Label Text="Endereço:" />
            <Entry x:Name="EnderecoEntry" />

            <Button Text="Cadastrar" 
                    x:Name="CadastrarButton" 
                    BackgroundColor="#ADD8E6" />

            <Button Text="Voltar" 
                    x:Name="VoltarButton" />
        </StackLayout>
    </ScrollView>
</ContentPage>
